import os import django os.environ.setdefault("DJANGO_SETTINGS_MODULE", "cgbookstore.settings") django.setup() from core.models import Book import pathlib print("=== INVESTIGACAO DOS 8 ARQUIVOS FALTANTES ===\n") titulos = [ "Kaiju", "Jujutsu", "Solo Leveling", "problema dos tres", "Calice de Fogo", "Morte no Nilo", "Codigo Limpo", "Pedra Filosofal" ] for busca in titulos: book = Book.objects.filter(title__icontains=busca).first() if book and book.cover_image: filename = book.cover_image.name.split("/")[-1] local_path = pathlib.Path(f"media/books/covers/{filename}") print(f"Livro: {book.title}") print(f" Arquivo DB: {filename}") print(f" Existe local: {local_path.exists()}") if local_path.exists(): print(f" Tamanho: {local_path.stat().st_size} bytes") print() 
